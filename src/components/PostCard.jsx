import React from 'react'
import appwriteService from "../appwrite/appconfig"
import {Link} from "react-router-dom"

function PostCard({$id, title , featuredImage}) {
  return (
    //checking error in git
    <Link to={``}/>
  )
}

export default PostCard